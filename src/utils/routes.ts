interface Routes {
  // Auth
  LOGIN: string;
  REGISTER: string;

  // Main
  HOME: string;
  PROFILE: string;
}

const routes: Routes = {
  // Auth
  LOGIN: 'Login',
  REGISTER: 'Register',

  // Main
  HOME: 'Home',
  PROFILE: 'Profile',
};

export default routes;
